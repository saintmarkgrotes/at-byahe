// Horizontal padding shared by every section on a screen
export const SCREEN_PADDING = 24;

// Space a scroll view must leave at the bottom so content clears the floating tab bar
export const TAB_BAR_HEIGHT = 92;
